import React from 'react'
import { View, Text } from 'react-native'

const ViewForm = (props) => {
    console.log('cargando de todo')
    console.log(props)
    return (
        <View>
            <Text> Cargando cosas </Text>
        </View>
    )
}

export default ViewForm
